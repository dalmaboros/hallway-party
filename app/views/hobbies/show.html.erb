<div class="max-w-3xl mx-auto">
  <h1 class="text-3xl mb-2">
    People who like <span class="font-bold"><%= @hobby_presenter.name %></span>
  </h1>

  <div>
    <%= render "membership_toggle",
          hobby_presenter: @hobby_presenter,
          user_hobby: @current_user_hobby,
          removable: @membership_removable %>
  </div>

  <% if @user_presenters.any? %>
    <ul class="divide-y divide-gray-200">
      <%= render partial: "attendees/attendee",
            collection: @user_presenters,
            as: :presenter %>
    </ul>
  <% else %>
    <div class="text-center py-16 text-gray-600">
      Nobody else lists <%= @hobby_presenter.name %> yet — be the conversation-starter.
    </div>
  <% end %>
</div>
