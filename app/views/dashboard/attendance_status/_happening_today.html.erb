<%# locals: (event_presenter:) %>
You're at <strong><%= event_presenter.name %></strong> <br>
Day <%= event_presenter.current_day %> of <%= event_presenter.total_days %> <br>
<%= link_to event_path(event_presenter.event),
      class: "font-bold underline hover:text-party-cyan" do %>
  See who here shares your interests! →
<% end %>
