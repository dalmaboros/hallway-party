<div class="max-w-2xl mx-auto">
  <h1 class="text-3xl font-bold mb-4">Dashboard</h1>
  <p class="mb-1">
    Signed in as <strong><%= @user_presenter.name %></strong>
    (<%= @user_presenter.username %>).
  </p>
  <%= link_to "View profile",
        profile_path(@user_presenter.username),
        class: "inline-block text-sm text-party-cyan hover:opacity-80 mb-6" %>

  <%= render "attendance_status", dashboard_events_presenter: @dashboard_events_presenter %>

  <%= render "events", dashboard_events_presenter: @dashboard_events_presenter %>

  <%= render "hobbies", hobby_presenters: @hobby_presenters %>
</div>
