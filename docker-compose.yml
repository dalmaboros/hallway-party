services:
  db:
    image: pgvector/pgvector:pg18
    environment:
      POSTGRES_USER: hallway_party
      POSTGRES_PASSWORD: hallway_party
      POSTGRES_DB: hallway_party_development
    ports:
      - "5432:5432"
    volumes:
      - hallway_party_pgdata:/var/lib/postgresql

volumes:
  hallway_party_pgdata:
